name: Build Android APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: |
          sdkmanager "platforms;android-35" "build-tools;35.0.0" "platform-tools"
          yes | sdkmanager --licenses >/dev/null

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Gradle build
        working-directory: android/AndroidStudioProject
        run: gradle build

      - name: Copy APK to web public directory
        run: node scripts/copy-apk.js

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: myclass-app-release
          path: |
            android/AndroidStudioProject/app/build/outputs/apk/release/app-release.apk
            web/public/myclass.apk
